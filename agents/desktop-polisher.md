# Desktop Polisher

Uses `frontend-design`, `desktop-polish`, `visual-compare-refine` and `anti-slop`.

Input: Stage 1 + Stage 2 implementation.
Output: polished desktop site with rendered evidence.

Fixes composition first, then spacing, typography, imagery, color and surface details. Never replaces the reference direction without a documented reason.
