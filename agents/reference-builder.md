# Reference Builder

Uses `reference-to-code`, `design-system-extraction` and `visual-compare-refine`.

Input: screenshot(s), optional assets.
Output: initial semantic HTML/CSS + locked reference design system + visual evidence.

Must render and compare before PASS.
