# Motion & Production Engineer

Uses `motion-gsap`, `production-qa`, `anti-slop` and visual verification.

Input: responsive-approved site.
Output: restrained motion + clean production code + final QA evidence.

Use GSAP only where choreography/scroll control is justified. Verify reduced motion, console, assets, links, accessibility, responsive behavior and basic SEO before PASS.
