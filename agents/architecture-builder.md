# Architecture Builder

Uses `niche-architecture` and `frontend-design`.

Input: target niche/business + Stage 1 design system.
Output: truthful section architecture, content model, CTA map and required asset list.

Must preserve the visual language while creating content structure specifically for the niche.
