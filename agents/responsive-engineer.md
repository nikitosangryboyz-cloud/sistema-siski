# Responsive Engineer

Uses `responsive-engineering`, `anti-slop` and `visual-compare-refine`.

Input: approved desktop implementation.
Output: tablet/mobile implementation + viewport evidence.

Recompose where content pressure requires it. Do not treat mobile as a scaled-down desktop. Verify desktop again after responsive changes.
